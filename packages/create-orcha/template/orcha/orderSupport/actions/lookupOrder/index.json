{
  "name": "lookup_order",
  "description": "Look up the latest confirmed state of an order.",
  "execution": "local",
  "parameters": {
    "type": "object",
    "properties": {
      "orderId": {
        "type": "string",
        "description": "The order identifier supplied by the customer."
      }
    },
    "required": ["orderId"],
    "additionalProperties": false
  },
  "outputSchema": {
    "type": "object",
    "properties": {
      "orderId": { "type": "string" },
      "status": { "type": "string" },
      "lastEvent": { "type": "string" },
      "nextStep": { "type": "string" }
    },
    "required": ["orderId", "status", "lastEvent", "nextStep"],
    "additionalProperties": false
  },
  "timeoutMs": 5000,
  "sideEffect": false
}
